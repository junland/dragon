
echo "Refreshing SSL certificate database ..."
chroot "$rootdir" usr/sbin/update-ca-certificates --fresh > /dev/null 2>&1

# Re-create symlink for cert.pem

(
    rm -f etc/ssl/cert.pem
    cd etc/ssl && ln -sf certs/ca-certificates.crt cert.pem
)

