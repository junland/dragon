
tqt3:

    - CUPS support.

    - NAS support.

avahi-tqt:

    - This requires avahi (common).

tdelibs:

    - Enable CUPS support.

tdeutils:

    - Option '-DWITH_SNMP=' depends on the netsnmp package.

    - Option 'DBUILD_KSIM=' is OFF by default, it does not compile
      with (current) tdeutils version 14.0.10.  Build error.
