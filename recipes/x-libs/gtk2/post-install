
# Update GTK+-2.0 modules cache
chroot "$rootdir" gtk-query-immodules-2.0 --update-cache

# Update GDK pixbuf loaders cache, just in case
chroot "$rootdir" /usr/bin/gdk-pixbuf-query-loaders --update-cache > /dev/null 2>&1

