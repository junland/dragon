
# This will be performed on the local filesystem
if test "$rootdir" = "" || test "$rootdir" = /
then
    echo "Rebuilding fontconfig cache: fc-cache -f --system-only"
    fc-cache -f --system-only
fi

