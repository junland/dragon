
# This will be performed on the local filesystem
if test "$rootdir" = "" || test "$rootdir" = /
then
    echo "Rebuilding fontconfig cache: fc-cache -r --system-only"
    fc-cache -r --system-only
fi

