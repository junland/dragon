#
# /etc/hosts.deny:
#
# This file describes the names of the hosts which are **not** allowed
# to use the local INET services, as decided by the '/usr/sbin/tcpd'
# server.
#
# Check the manual pages for hosts_access(5), and hosts_options(5).
#

# The default policy.  This denies all service to all hosts, unless
# they are permitted access by entries in the /etc/hosts.allow file
ALL: ALL
