#
# /etc/hosts.allow:
#
# This file describes the names of the hosts which are allowed to use
# the local INET services, as decided by the '/usr/sbin/tcpd' server.
#
# Check the manual pages for hosts_access(5), and hosts_options(5).
#
